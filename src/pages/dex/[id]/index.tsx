import { DexEntry } from "@/components/PageComponents/Dex/DexEntry";
import { Page } from "../../../components/PageComponents";

const DexPageId = () => {
  return (
    <Page>
      <DexEntry />
    </Page>
  );
};

export default DexPageId;
